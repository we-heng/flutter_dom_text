name: dom_text
description: "DomText is a widget modeled after Flutter's Text widget. It uses HtmlElementView to render text as real HTML."
version: 0.0.2
homepage: https://github.com/we-heng/flutter_dom_text
repository: https://github.com/we-heng/flutter_dom_text

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
